#include <stdio.h>

int main() {
    int num = -5;

    if("입력한 수는 %s\n", (num % 2 == 0) ? "짝수" : "홀수"); {

    }

       
    if (num > 0) {
        printf("양수\n");
    } else (num < 0) {
        printf("짝수\n");
    }
        

    return 0;
}