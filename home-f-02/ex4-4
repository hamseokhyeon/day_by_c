#include <stdio.h>

int main(){
    int x;
    printf("정수를 입력하세요: ");
    scanf("%d", &x);

    if (x % 2 == 0) {
        printf("%d는 짝수입니다.\n", x);
    } else {
        printf("%d는 홀수입니다.\n", x);
    }
    return 0;
}